node_modules

# .NET build output
**/bin/
**/obj/

# Paquetes de publicación (no subir al repo — pueden contener secretos)
**/publish/
**/publish_deploy/

# Base de datos local (no subir al repo; en producción se crea en /data)
*.db
*.db-shm
*.db-wal

# Fotos subidas localmente
backend/InformesTecnicos.Api/uploads/

# Variables de entorno locales
.env
.env.local

# Credenciales / secretos — NUNCA subir al repo (la plantilla .example sí se sube)
appsettings.Secrets.json
**/appsettings.Secrets.json

# Visual Studio / Rider workspace temp files
.vs/
backend/.vs/
*.user
*.suo
.idea/

# Ejecutables e instaladores (nunca al repo)
*.exe
*.msi
*.dmg
*.pkg
*.deb
*.rpm
*.appimage

# Frontend build output
frontend/dist/
frontend/.vite/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
npm-debug.log*
